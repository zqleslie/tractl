# Desktop frontend assets (generated)

This directory is **not** the shared UI source.

- **Source:** repository root `frontend/`
- **Generated output:** `dist/` (produced by `npm run build:desktop` in `frontend/`)
- **Consumed by:** `//go:embed frontend/dist` in `cmd/desktop/main.go`

Do not add React source files here.
