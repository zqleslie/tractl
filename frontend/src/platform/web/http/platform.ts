// ADR-016 amendment — Web Tier 2 transport path (server-connected mode).
// This module will route engine calls to a running traCtl server via fetch()
// when the user opts in to connected mode from Settings.
// Not yet implemented — placeholder satisfies the ADR-016 platform layer structure.
// Implement when Tier 2 connected mode is added (post Phase UI-0.3).

export {}
